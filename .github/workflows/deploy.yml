name: Deploy to Vercel

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy
        run: |
          curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_bofPHtEy9SqYNP5C16tG0pWCHhLS/HCpdFLLWE4"
